# Hosted Postgres used by the Clients page. Copy to .env.local and set the value.
# Any Postgres provider works. Include sslmode=require for hosted databases, e.g.
# postgresql://user:password@host/database?sslmode=require
DATABASE_URL=
